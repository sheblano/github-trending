# Screenshots for README / marketing

Add hero images here so the repo README can link to stable paths:

| File | Suggested capture |
|------|-------------------|
| `galaxy-dark.png` | Galaxy discovery view, dark theme, weekly range |
| `movers-dark.png` | Top Movers dashboard with at least one section populated |
| `trending-dark.png` | Trending page with radar or default view |

After capturing, reference them from the root [README.md](../../README.md), for example:

```markdown
![Galaxy discovery](docs/screenshots/galaxy-dark.png)
```

Tip: use the browser’s device toolbar for a consistent width (e.g. 1280px).
